import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0C0C0E);
  static const surface    = Color(0xFF141416); // mid layer between bg and card
  static const card       = Color(0xFF1A1A1E); // slightly refined
  static const cardHigh   = Color(0xFF222228); // elevated surface (top of glass)
  static const border     = Color(0x0DFFFFFF); // 5 % white — softer

  static const textPrimary   = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8E8E93);
  static const textTertiary  = Color(0xFF48484A);

  static const brand = Color(0xFF3D5AFE);
  static const green = Color(0xFF30D158); // brighter Apple-green
  static const red   = Color(0xFFFF453A); // brighter Apple-red
  static const amber = Color(0xFFFFD60A); // favorites
  static const gray  = Color(0xFF2C2C2E);
}