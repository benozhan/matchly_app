import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('ÇALIŞIYOR', style: TextStyle(fontSize: 32, color: Colors.black)),
      ),
    ),
  ));
}
